<UserControl x:Class="QvrProClient.WpfTester.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Host:" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Host, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Port:" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Port, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <CheckBox Grid.Row="2" Grid.Column="1" Content="Use HTTPS" IsChecked="{Binding UseHttps}" Margin="0,0,0,8" />
        <CheckBox Grid.Row="3" Grid.Column="1" Content="Ignore Certificate" IsChecked="{Binding IgnoreCert}" Margin="0,0,0,8" />

        <TextBlock Grid.Row="4" Grid.Column="0" Text="Username:" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Password:" Margin="0,0,8,8" VerticalAlignment="Center" />
        <PasswordBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,8"
                     PasswordChanged="PasswordBox_OnPasswordChanged" />

        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" Margin="0,4,0,8">
            <Button Content="Login" Command="{Binding LoginCommand}" Width="100" Margin="0,0,8,0" />
            <Button Content="Logout" Command="{Binding LogoutCommand}" Width="100" />
        </StackPanel>

        <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding IsLoggedIn, StringFormat=Is Logged In: {0}}" />
        <TextBlock Grid.Row="8" Grid.Column="0" Grid.ColumnSpan="2" Foreground="DarkRed" Text="{Binding ErrorMessage}" />
    </Grid>
</UserControl>
