<UserControl x:Class="QvrProClient.WpfTester.Views.PlaybackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Camera:" Margin="0,0,8,8" VerticalAlignment="Center" />
        <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding Cameras}" SelectedItem="{Binding SelectedCamera}" DisplayMemberPath="Name" Margin="0,0,0,8" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Start (ISO):" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding StartTimeText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <TextBlock Grid.Row="2" Grid.Column="0" Text="End (ISO):" Margin="0,0,8,8" VerticalAlignment="Center" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding EndTimeText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8" />

        <StackPanel Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2">
            <Button Content="Open Playback" Command="{Binding OpenPlaybackCommand}" Width="140" HorizontalAlignment="Left" Margin="0,0,0,8" />
            <TextBox Text="{Binding ResultText}" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" Height="120" />
        </StackPanel>
    </Grid>
</UserControl>
